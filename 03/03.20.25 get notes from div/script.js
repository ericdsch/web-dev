let wrapper = document.querySelector('.wrapper')

//arrays have a special filter?

//toggle is a method not a property, methods are more like functions, transforming the data -- color is a property 
//look up instructions and tags 

let changeColor = () => {
    console.log("clocked");
}
games.addEventListener("click",() => {})

//look at inbuilt html tags and options 
//check out github repository to see button display and selections and how to upload a .csv file 